spring.application.name=reachscanner
server.port=8080

# Logging
logging.level.com.reachai.reachscanner=INFO

# Temporary directory for cloned repos
reachscanner.clone.base-dir=${java.io.tmpdir}/reachscanner-repos