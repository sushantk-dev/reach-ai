info:
  name: scanner
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/scans
  body:
    type: json
    data: |-
      {
        "repoUrl": "https://github.com/sushantk-dev/vuln-app"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
